import { Box, styled, TextField } from '@mui/material';

export const WrapperStyled = styled(Box)`
    margin: 20px 0;
    display: flex;
    align-items: center;
`;

export const TextFieldStyled = styled(TextField)`
    width: 400px;
    padding: 10px;
`;

export const ButtonsStyled = styled(Box)`
    display: flex;
    gap: 10px;
`;
